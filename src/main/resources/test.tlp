let fact = (num of Number) of Number {
    if num <= 1 {
        1
    }
    num * :self(num - 1)    // Use ":self" to refer to the current function recursively
                            // In this example, "fact" cannot be used because functions are just variables and the variable is not yet defined
};

let factorialExample = fact(5);     // Number: 120
